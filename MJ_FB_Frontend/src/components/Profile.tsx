export default function Profile({ token }: { token: string }) {
    return (
      <div>
        <h2>User Profile</h2>
        <p>Show user info, booking history, etc. here.</p>
      </div>
    );
  }